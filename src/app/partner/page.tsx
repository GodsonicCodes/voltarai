import React from "react";
import PartnerForm from "@/components/PartnerForm";

const page = () => {
    return (
        <div>
            <PartnerForm />
        </div>
    );
};

export default page;
